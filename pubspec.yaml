name: app_cadastro
description: "Aplicativo de agendamento de serviços com Supabase"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  flutter_secure_storage: ^9.2.4
  rxdart: ^0.28.0
  mime: ^2.0.0
  app_links: ^6.4.0
  intl: ^0.20.2  # Atualizado para versão mais recente

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true